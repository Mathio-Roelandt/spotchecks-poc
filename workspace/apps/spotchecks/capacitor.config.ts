import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.houseofhr.spotchecks',
  appName: 'spotchecks',
  webDir: '../../dist/apps/spotchecks',
};

export default config;
